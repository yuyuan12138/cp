#include <bits/stdc++.h>

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  int a, b, n;
  std::cin >> a >> b >> n;
  std::vector<std::vector<int>> s(a + 1, std::vector<int> (b + 1));
  for (int i = 0; i < n; i++) {
  }
  return 0;
}
