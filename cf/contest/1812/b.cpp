#include <bits/stdc++.h>

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int n;
    std::cin >> n;
    if (n == 15 || n == 20 || n == 21) {
        std::cout << "NO\n";
    } else {
        std::cout << "YES\n";
    }
    return 0;
}